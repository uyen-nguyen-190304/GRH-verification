# ------------------------------------------------------------------
#  Python dependencies for GRH-verification
# ------------------------------------------------------------------
#  Versions below are the ones I tested with; newer versions should
#  work too.  All packages are pure-Python wheels so installation is
#  fast and platform-independent
# ------------------------------------------------------------------

numpy>=1.24          # array math, .npy cache, format_float_positional
pybind11>=2.11       # C++/Python binding headers (needed at build time)
